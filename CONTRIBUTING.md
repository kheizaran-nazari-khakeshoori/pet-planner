# Contributing to Pet Planner

Thanks for your interest in contributing. A few guidelines:

- Fork the repository and create a branch for your changes.
- Run the test suite with `pytest` before opening a pull request.
- Keep changes focused and add unit tests for new behavior.
- Follow existing code style and keep commits small and descriptive.

If your change affects the database schema, include a migration or update `init_db.py`.
